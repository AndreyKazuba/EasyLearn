<Page x:Class="EasyLearn.UI.Pages.EditCommonDictionaryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:constants="clr-namespace:EasyLearn.Data.Constants;assembly=EasyLearn.Data"
      xmlns:valudationRules="clr-namespace:EasyLearn.Infrastructure.Validation"
      mc:Ignorable="d" 
      xmlns:converters="clr-namespace:EasyLearn.Infrastructure.ValueConverters"
      d:DesignHeight="450" d:DesignWidth="800">
    <materialDesign:DialogHost OverlayBackground="{DynamicResource MainColorSuperDark}">
        <Grid >

            <!--Кнопка "удалить все CommonRelations"-->
            <!--<Button VerticalAlignment="Center" 
                        Command="{Binding ClearCommonDictionaryCommand}"
                        Style="{DynamicResource MaterialDesignFlatButton}"
                        Content="Очистить"
                        DockPanel.Dock="Right"
                        Foreground="#FFA70404"
                        Margin="5 0"/>-->

            <!--Поисковая строка-->
            <!--<materialDesign:ColorZone Mode="Standard"
                                          Height="40"
                                          CornerRadius="6"
                                          materialDesign:ShadowAssist.ShadowDepth="Depth1"
                                          Margin="2"
                                          VerticalAlignment="Center"
                                          Width="260">
                    <DockPanel LastChildFill="True">
                        <Button Style="{StaticResource MaterialDesignToolForegroundButton}"
                                Margin="8 -6 8 -6"
                                DockPanel.Dock="Right"
                                Content="{materialDesign:PackIcon Kind=Search}"/>
                        <TextBox FontSize="15"
                                 Margin="8 0 0 0"
                                 VerticalAlignment="Center"
                                 materialDesign:HintAssist.Hint="Search"
                                 Text="{Binding SearchStringValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">



                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="TextChanged">
                                    <i:InvokeCommandAction Command="{Binding SearchCommonRelationsCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>

                        </TextBox>
                    </DockPanel>
                </materialDesign:ColorZone>-->


            <materialDesign:DrawerHost OpenMode="Standard" materialDesign:ShadowAssist.ShadowDepth="Depth0">
                <materialDesign:DrawerHost.TopDrawerContent>
                    <Border Background="GhostWhite" Height="58" BorderBrush="{DynamicResource MainColor}" BorderThickness="0.6 0 0 0.6">
                        <Grid VerticalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="245"/>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="60"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Style="{DynamicResource EasyTextBox}"
                                     Width="245"
                                     Margin="0"
                                     Grid.Column="1"
                                     Padding="14 11"
                                     VerticalAlignment="Center"
                                     InputLanguageManager.InputLanguage="ru-RU"
                                     materialDesign:HintAssist.Hint="Искать"
                                     materialDesign:HintAssist.IsFloating="False"
                                     Text="{Binding SearchStringValue, UpdateSourceTrigger=PropertyChanged}">


                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="TextChanged">
                                        <i:InvokeCommandAction Command="{Binding CheckCommonRelationForExistingCommand}"/>
                                        <i:InvokeCommandAction Command="{Binding UpdateConfirmCommonRelationAddingButtonAvailabilityCommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                            </TextBox>

                            <Button Style="{StaticResource EasyCommonButton}" Margin="10 0 0 0" Height="38" 
                                   Grid.Column="2" Command="{Binding SearchCommonRelationsCommand}">
                                <TextBlock Text="Искать" FontSize="14" FontFamily="Arial"/>

                            </Button>

                            <Button Style="{StaticResource EasyCancelButton}" Margin="0 0 15 0" Height="38" Width="38"
                                    Grid.Column="4"
                                    Padding="0">
                                <materialDesign:PackIcon Kind="TrashCanOutline" Height="28" Width="28"
                                                     VerticalAlignment="Center" Foreground="{DynamicResource EasyRed}"
                                                     HorizontalAlignment="Center"/>
                            </Button>
                            
                            

                        </Grid>
                    </Border>
                </materialDesign:DrawerHost.TopDrawerContent>
                <Grid>
                    <Button Visibility="Collapsed" Name="openMenuButton" Command="{x:Static materialDesign:DrawerHost.OpenDrawerCommand}"/>
                    <Button Visibility="Collapsed" Name="closeMenuButton" Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}"/>
                    <!--Контейнер для содержания всех CommonRelations-->
                    <ScrollViewer Grid.Row="1" ScrollViewer.CanContentScroll="False">
                        <ItemsControl ItemsSource="{Binding CommonRelationViews}" 
                                  VerticalContentAlignment="Top"
                                  ScrollViewer.CanContentScroll="False"
                                  HorizontalContentAlignment="Left">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
                
            </materialDesign:DrawerHost>



            <Button Name="openCommonRelationSettingsWindowButton"
                    Visibility="Collapsed"
                    Command="{x:Static materialDesign:DialogHost.OpenDialogCommand}">

                <Button.CommandParameter>
                    <Grid Margin="0 14">

                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition Height="Auto" MaxHeight="0.8"/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition Height="Auto" MaxHeight="0.8"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <Grid Margin="14 0">
                            <!--Текстбокс для введения комментария к новому CommonRelation-->
                            <TextBox Style="{DynamicResource EasyValidationTextBox}"
                                     Width="260" 
                                     Grid.Row="7"
                                     HorizontalAlignment="Left"
                                 materialDesign:HintAssist.IsFloating="False"
                                 materialDesign:ValidationAssist.Suppress="True"
                                     Padding="14 11"
                                     Grid.ColumnSpan="3"
                                     InputLanguageManager.InputLanguage="ru-RU"
                                     KeyDown="OnCommentValueTextBoxKeyDown"
                                     materialDesign:HintAssist.Hint="Комментарий">

                                <TextBox.Text>
                                    <Binding Path="UwCommentValue" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <valudationRules:InRange Max="{x:Static constants:ModelConstants.CommonRelationCommentMaxLength}" Group="UpdateCommonRelation" ValidatesOnTargetUpdated="True"/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>

                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="TextChanged">
                                        <i:InvokeCommandAction Command="{Binding UwUpdateConfirmButtonAvailabilityCommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                            </TextBox>

                            <Button Style="{StaticResource EasyCancelButton}" Height="38" Width="38"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Margin="0 0 0 14"
                                    Padding="0">
                                <materialDesign:PackIcon Kind="TrashCanOutline" Height="28" Width="28" Foreground="{DynamicResource EasyRed}"
                                                     VerticalAlignment="Center"
                                                     HorizontalAlignment="Center"/>
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Click">
                                        <i:InvokeCommandAction Command="{Binding DeleteCommonRelationCommand}"/>
                                        <i:InvokeCommandAction Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </Button>
                        </Grid>

                        <Border BorderBrush="{DynamicResource EasyGray}" 
                                    BorderThickness="0.5"
                                    Grid.Row="1"/>

                        <Grid Grid.Row="2"
                              Margin="14 12 14 0"
                                  Grid.Column="0"
                                  Grid.ColumnSpan="3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="0.12*"/>
                            </Grid.ColumnDefinitions>

                            <TextBox Style="{DynamicResource EasyTextBox}"
                                         Width="192"
                                         Padding="14 11"
                                         Margin="0 0 7 0"
                                 materialDesign:HintAssist.IsFloating="False"
                                     
                                         KeyDown="OnUpdateWindowExampleRussianValueTextBoxKeyDown"
                                         InputLanguageManager.InputLanguage="ru-RU"
                                         Name="uwExampleRussianValueField"
                                         materialDesign:HintAssist.Hint="Пример на русском">

                                <TextBox.Text>
                                    <Binding Path="UwExampleRussianValue" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <valudationRules:InRange Max="{x:Static constants:ModelConstants.ExampleValueMaxLength}" Group="UpdateCommonRelation" ValidatesOnTargetUpdated="True"/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>

                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="TextChanged">
                                        <i:InvokeCommandAction Command="{Binding UwValidateExampleSectionCommand}"/>
                                        <i:InvokeCommandAction Command="{Binding UwCheckExampleFieldsMaxLengthVisibilityCommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </TextBox>

                            <TextBox Style="{DynamicResource EasyTextBox}"
                                         HorizontalAlignment="Stretch"
                                         Width="192"
                                         Grid.Column="1"
                                         Margin="7 0 0 0"
                                         Padding="14 11"
                                 materialDesign:HintAssist.IsFloating="False"
                                     
                                         KeyDown="OnUpdateWindowExampleEnglishValueTextBoxKeyDown"
                                         Name="uwExampleEnglishValueField"
                                         InputLanguageManager.InputLanguage="en-US"
                                         materialDesign:HintAssist.Hint="Пример на английском">

                                <TextBox.Text>
                                    <Binding Path="UwExampleEnglishValue" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <valudationRules:InRange Max="{x:Static constants:ModelConstants.ExampleValueMaxLength}" Group="UpdateCommonRelation" ValidatesOnTargetUpdated="True"/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>

                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="TextChanged">
                                        <i:InvokeCommandAction Command="{Binding UwValidateExampleSectionCommand}"/>
                                        <i:InvokeCommandAction Command="{Binding UwCheckExampleFieldsMaxLengthVisibilityCommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </TextBox>

                            <Button Style="{DynamicResource EasyConfirmButton}"
                                        Foreground="{DynamicResource green}"
                                        Height="39"
                                        Width="39"
                                        Margin="14 0 0 0"
                                        IsEnabled="{Binding UwAddExampleButtonIsEnabled}"
                                        Name="uwAddExampleButton"
                                        HorizontalAlignment="Center" VerticalAlignment="Top"
                                        Grid.Column="2">
                                <materialDesign:PackIcon Kind="Add" Foreground="{DynamicResource EasyGreen}"/>

                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Click">
                                        <i:InvokeCommandAction Command="{Binding UwAddExampleViewCommand}"/>
                                        <i:InvokeCommandAction Command="{Binding UwClearExampleAddingSectionCommand}"/>
                                        <i:InvokeCommandAction Command="{Binding UwFocusExampleRussianValueFieldCommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </Button>

                        </Grid>

                        <ItemsControl ItemsSource="{Binding UwExampleViews}" 
                                          Grid.Row="3"
                                          Grid.Column="0"
                                          Margin="0 0 0 14"
                                          Width="450"
                                          Grid.ColumnSpan="3">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>


                        <Border BorderBrush="{DynamicResource EasyGray}" 
                                    BorderThickness="0.5"
                                    Grid.Row="4"/>

                        <!--Кнопки отмены и подтверждения добавления нового CommonRelation-->
                        <Grid Grid.Row="5"
                                  Width="230"
                              Margin="14 12 14 0"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <!--Кнопка отмены-->
                            <Button Style="{DynamicResource EasyCancelButton}"
                                    Name="newVerbPrepositionAddingWindowCancelButton">

                                <TextBlock Text="Отмена" FontSize="14" FontFamily="Arial"/>

                                <!--Действия кнопки при её нажатии-->
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Click">
                                        <i:InvokeCommandAction Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                            </Button>

                            <!--Кнопка подтверждения-->
                            <Button Style="{DynamicResource EasyConfirmButton}"
                                    Grid.Column="1"
                                    Name="newVerbPrepositionAddingButton"
                                    IsEnabled="{Binding IsConfirmVerbPrepositionAddingButtonEnabled}">

                                <TextBlock Text="Сохранить" FontSize="14" FontFamily="Arial"/>

                                <!--Действия кнопки при её нажатии-->
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Click">
                                        <i:InvokeCommandAction Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"/>
                                        <i:InvokeCommandAction Command="{Binding UpdateCommonRelationCommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                            </Button>

                        </Grid>

                    </Grid>
                </Button.CommandParameter>

            </Button>

            <!--Кнопка добавления нового CommonRelation-->
            <Button Command="{x:Static materialDesign:DialogHost.OpenDialogCommand}"
                        Name="openNewCommonRelationAddingWindowButton"
                        Visibility="Collapsed">

                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:InvokeCommandAction Command="{Binding AwClearCommand}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>

                <!--Диалоговое окно добавления нового CommonRelation-->
                <Button.CommandParameter>
                    <Grid Margin="0 14">

                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition Height="Auto" MaxHeight="0.8"/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition Height="Auto"  MaxHeight="0.8"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="Auto" MaxWidth="0.8"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <!--Текстбокс для введения русского значения в новом CommonRelation-->
                        <TextBox Style="{DynamicResource EasyValidationTextBox}"
                                     Width="215"
                                     Padding="14 11"
                                 Margin="14 0 0 14"
                                     InputLanguageManager.InputLanguage="ru-RU"
                                 materialDesign:ValidationAssist.Suppress="True"
                                 materialDesign:HintAssist.IsFloating="False"
                                     KeyDown="OnRussianValueTextBoxKeyDown"
                                     Name="newCommonRelationRussianValueTextBox"
                                     materialDesign:HintAssist.Hint="Русский перевод*">

                            <TextBox.Text>
                                <Binding Path="AwRussianValue" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <valudationRules:NotEmpty Group="AddCommonRelation" ValidatesOnTargetUpdated="True" />
                                        <valudationRules:InRange Max="{x:Static constants:ModelConstants.UnitValueMaxLength}" Group="AddCommonRelation" ValidatesOnTargetUpdated="True"/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="TextChanged">
                                    <i:InvokeCommandAction Command="{Binding AwCheckCommonRelationForExistingCommand}"/>
                                    <i:InvokeCommandAction Command="{Binding AwUpdateConfirmButtonAvailabilityCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>

                        </TextBox>

                        <!--Комбобокс для выбора типа RussianUnit в новом CommonRelation-->
                        <ComboBox MaxDropDownHeight="200"
                                      Grid.Row="1"
                                      Margin="14 0 0 14"
                                      Width="215"
                                      Padding="14 11"
                                      Grid.Column="0"
                                      KeyDown="OnRussianUnitTypeComboBoxKeyDown"
                                      Name="newCommonRelationRussianUnitTypeComboBox"
                                      ItemsSource="{Binding AwRussianUnitTypes}"
                                      SelectedItem="{Binding AwSelectedRussianUnitType, Mode=TwoWay}"
                                      Style="{DynamicResource EasyComboBox}">

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding AwCheckCommonRelationForExistingCommand}"/>
                                    <i:InvokeCommandAction Command="{Binding AwUpdateConfirmButtonAvailabilityCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>

                        </ComboBox>

                        <Border BorderBrush="{DynamicResource EasyGray}" 
                                    BorderThickness="0.5"
                                    Margin="0 0 0 14"
                                    Grid.RowSpan="2"
                                    Grid.Column="1"/>

                        <!--Текстбокс для введения английского значения в новом CommonRelation-->
                        <TextBox Style="{DynamicResource EasyValidationTextBox}"
                                     Width="215"
                                 Margin="0 0 14 14"
                                     Padding="14 11"
                                 materialDesign:ValidationAssist.Suppress="True"
                                     Grid.Column="2"
                                     InputLanguageManager.InputLanguage="en-US"
                                     KeyDown="OnEnglishValueTextBoxKeyDown"  
                                     materialDesign:ValidationAssist.PopupPlacement="AbsolutePoint"
                                     Name="newCommonRelationEnglishValueTextBox"
                                     materialDesign:HintAssist.Hint="Английский перевод*"
                                 materialDesign:HintAssist.IsFloating="False">

                            <TextBox.Text>
                                <Binding Path="AwEnglishValue" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <valudationRules:NotEmpty Group="AddCommonRelation" ValidatesOnTargetUpdated="True" />
                                        <valudationRules:InRange Max="{x:Static constants:ModelConstants.UnitValueMaxLength}" Group="AddCommonRelation" ValidatesOnTargetUpdated="True"/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="TextChanged">
                                    <i:InvokeCommandAction Command="{Binding AwCheckCommonRelationForExistingCommand}"/>
                                    <i:InvokeCommandAction Command="{Binding AwUpdateConfirmButtonAvailabilityCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>

                        </TextBox>

                        <!--Комбобокс для выбора типа EnglishUnit в новом CommonRelation-->
                        <ComboBox MaxDropDownHeight="200"
                                      Grid.Row="1"
                                      Grid.Column="2"
                                      Margin="0 0 14 14"
                                      Width="215"
                                      Padding="14 11"
                                      KeyDown="OnEnglishUnitTypeComboBoxKeyDown"
                                      Name="newCommonRelationEnglishUnitTypeComboBox"
                                      ItemsSource="{Binding AwEnglishUnitTypes}"
                                      SelectedItem="{Binding AwSelectedEnglishUnitType, Mode=TwoWay}"
                                      Style="{DynamicResource EasyComboBox}">

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding AwCheckCommonRelationForExistingCommand}"/>
                                    <i:InvokeCommandAction Command="{Binding AwUpdateConfirmButtonAvailabilityCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>

                        </ComboBox>

                        <!--Текстбокс для введения комментария к новому CommonRelation-->
                        <TextBox Style="{DynamicResource EasyValidationTextBox}"
                                     Width="230" 
                                     Grid.Row="2"
                                 materialDesign:HintAssist.IsFloating="False"
                                 materialDesign:ValidationAssist.Suppress="True"
                                     Padding="14 11"
                                     Grid.ColumnSpan="3"
                                     InputLanguageManager.InputLanguage="ru-RU"
                                     Name="newCommonRelationCommentValueTextBox"
                                     KeyDown="OnCommentValueTextBoxKeyDown"
                                     materialDesign:HintAssist.Hint="Комментарий">

                            <TextBox.Text>
                                <Binding Path="AwCommentValue" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <valudationRules:InRange Max="{x:Static constants:ModelConstants.CommonRelationCommentMaxLength}" Group="AddCommonRelation" ValidatesOnTargetUpdated="True"/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="TextChanged">
                                    <i:InvokeCommandAction Command="{Binding UpdateConfirmCommonRelationAddingButtonAvailabilityCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>

                        </TextBox>

                        <!--Надпись о том, что запись уже существует-->
                        <StackPanel VerticalAlignment="Center"
                                        Margin="0 -10 0 0"
                                        HorizontalAlignment="Center" 
                                        Grid.Row="3" Grid.ColumnSpan="3" 
                                        Orientation="Horizontal" 
                                        Visibility="{Binding AwCommonRelationHasExistLableIsVisible, Converter={converters:ReverseCollapseConverter}}">
                            <materialDesign:PackIcon Kind="AlertCircleOutline" Foreground="{DynamicResource error}" Margin="2"/>
                            <TextBlock VerticalAlignment="Center"
                                           Margin="2 0 0 2"
                                           Text="Такая запись уже есть" 
                                           Foreground="{DynamicResource error}"
                                           Style="{DynamicResource MaterialDesignSubtitle2TextBlock}"/>
                            <TextBlock/>
                        </StackPanel>

                        <Border BorderBrush="{Binding HorisontalSeporatorColor}" 
                                    BorderThickness="0.5" 
                                    Grid.ColumnSpan="3"
                                    Grid.Row="4"
                                    Grid.Column="0"/>

                        <!--Строка с добавлением примеров-->
                        <Grid Grid.Row="5"
                                  Grid.Column="0"
                                  Grid.ColumnSpan="3"
                              Margin="14 14 14 0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="0.12*"/>
                            </Grid.ColumnDefinitions>

                            <TextBox Style="{DynamicResource EasyTextBox}"
                                         Width="188"
                                         Padding="14 11"
                                         Margin="0 0 7 0"
                                 materialDesign:HintAssist.IsFloating="False"
                                     
                                         KeyDown="OnAddingWindowExampleRussianValueTextBoxKeyDown"
                                         InputLanguageManager.InputLanguage="ru-RU"
                                         Name="awExampleRussianValueField"
                                         materialDesign:HintAssist.Hint="Пример на русском">

                                <TextBox.Text>
                                    <Binding Path="AwExampleRussianValue" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <valudationRules:InRange Max="{x:Static constants:ModelConstants.ExampleValueMaxLength}" Group="AddCommonRelation" ValidatesOnTargetUpdated="True"/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>

                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="TextChanged">
                                        <i:InvokeCommandAction Command="{Binding AwValidateExampleSectionCommand}"/>
                                        <i:InvokeCommandAction Command="{Binding AwCheckExampleFieldsMaxLengthVisibilityCommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </TextBox>

                            <TextBox Style="{DynamicResource EasyTextBox}"
                                         HorizontalAlignment="Stretch"
                                         Width="188"
                                         Grid.Column="1"
                                         Margin="7 0 0 0"
                                         Padding="14 11"
                                 materialDesign:HintAssist.IsFloating="False"
                                     
                                         KeyDown="OnAddingWindowExampleEnglishValueTextBoxKeyDown"
                                         Name="awExampleEnglishValueField"
                                         InputLanguageManager.InputLanguage="en-US"
                                         materialDesign:HintAssist.Hint="Пример на английском">

                                <TextBox.Text>
                                    <Binding Path="AwExampleEnglishValue" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <valudationRules:InRange Max="{x:Static constants:ModelConstants.ExampleValueMaxLength}" Group="AddCommonRelation" ValidatesOnTargetUpdated="True"/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>

                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="TextChanged">
                                        <i:InvokeCommandAction Command="{Binding AwValidateExampleSectionCommand}"/>
                                        <i:InvokeCommandAction Command="{Binding AwCheckExampleFieldsMaxLengthVisibilityCommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </TextBox>

                            <Button Style="{DynamicResource EasyConfirmButton}"
                                        Foreground="{DynamicResource green}"
                                        Height="39"
                                        Width="39"
                                        Margin="14 0 0 0"
                                        IsEnabled="{Binding AwAddExampleButtonIsEnabled}"
                                        Name="awAddExampleButton"
                                        HorizontalAlignment="Center" VerticalAlignment="Top"
                                        Grid.Column="2">
                                <materialDesign:PackIcon Kind="Add" Foreground="{DynamicResource EasyGreen}" />

                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Click">
                                        <i:InvokeCommandAction Command="{Binding AwAddExampleViewCommand}"/>
                                        <i:InvokeCommandAction Command="{Binding AwClearExampleAddingSectionCommand}"/>
                                        <i:InvokeCommandAction Command="{Binding AwFocusExampleRussianValueFieldCommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </Button>

                        </Grid>

                        <ItemsControl ItemsSource="{Binding AwExampleViews}" 
                                          Grid.Row="6"
                                          Grid.Column="0"
                                          Margin="14 0 14 14"
                                          Width="450"
                                          Grid.ColumnSpan="3">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>

                        <Border BorderBrush="{DynamicResource EasyGray}" 
                                    BorderThickness="0.5" 
                                    Grid.ColumnSpan="3"
                                    Grid.Row="7"
                                    Grid.Column="0"/>

                        

                        <!--Кнопки отмены и подтверждения добавления нового CommonRelation-->
                        <Grid Grid.Row="8"
                                  Grid.Column="0"
                                  Width="230"
                              Margin="0 14 0 0"
                                  Grid.ColumnSpan="3"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <!--Кнопка отмены добавления нового CommonRelation-->
                            <Button Style="{DynamicResource EasyCancelButton}"
                                    Name="newCommonRelationAddingWindowCancelButton">


                                <TextBlock Text="Отмена" FontSize="14" FontFamily="Arial"/>

                                <!--Действия кнопки при её нажатии-->
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Click">
                                        <i:InvokeCommandAction Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"/>
                                        <i:InvokeCommandAction Command="{Binding AwClearCommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                            </Button>

                            <!--Кнопка подтверждения добавления нового CommonRalation-->
                            <Button Style="{DynamicResource EasyConfirmButton}"
                                    Grid.Column="1"
                                    Name="newCommonRelationAddingButton"
                                    IsEnabled="{Binding AwConfirmButtonIsEnabled}">

                                <TextBlock Text="Добавить" FontSize="14" FontFamily="Arial"/>

                                <!--Действия кнопки при её нажатии-->
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Click">
                                        <i:InvokeCommandAction Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"/>
                                        <i:InvokeCommandAction Command="{Binding CreateCommonRelationCommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                            </Button>

                        </Grid>

                    </Grid>

                </Button.CommandParameter>

            </Button>

        </Grid>
    </materialDesign:DialogHost>
</Page>
